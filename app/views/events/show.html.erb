<h2>申請中のバンド一覧</h2>

<p><strong>基本出演料:</strong> <%= "#{@event.base_fee} 円" %><br>


<% if @bands.present? && @bands.any? %>
  <table>
    <thead>
      <tr>
        <th>バンド名</th>
        <th>リーダー</th>
        <th>注釈</th>
      </tr>
    </thead>
    <tbody>
      <% @bands.each do |band| %>
        <tr>
          <td><%= band.name %></td>
          <td><%= band.leader.name %></td>
          <td><%= band.notes %></td>
          <% if current_user.admin? %>
            <td>
            <%= link_to "削除", admin_delete_band_path(band), data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger" %>
            </td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>現在、このイベントに申請中のバンドはありません。</p>
<% end %>

<%= link_to "イベント一覧に戻る", events_path, class: "btn btn-secondary", style: "margin-top: 10px;" %>

