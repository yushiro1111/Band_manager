<h1><%= @event.name %> に申請中のバンド</h1>

<p>開催日: <%= @event.date.strftime("%Y年%m月%d日 %H:%M") %></p>
<p>場所: <%= @event.location %></p>

<% if @bands.any? %>
  <table>
    <thead>
      <tr>
        <th>バンド名</th>
        <th>リーダー</th>
        <th>メンバー</th>
        <th>注釈</th>
      </tr>
    </thead>
    <tbody>
      <% @bands.each do |band| %>
        <tr>
          <td><%= band.name %></td>
          <td><%= band.leader.name %></td>
          <td>
            <%= [band.vocal, band.guitar1, band.guitar2, band.bass, band.drum, band.keyboard]
                 .compact.join(", ") %>
          </td>
          <td><%= band.notes %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>現在、このイベントに申請中のバンドはありません。</p>
<% end %>

<%= link_to "イベント一覧に戻る", events_path, class: "btn btn-secondary", style: "margin-top: 10px;" %>
